# LINE LIFF
VITE_LIFF_ID=

# Backend API base URL (leave empty in dev — Vite proxy forwards /api to localhost:8080)
VITE_API_BASE_URL=

# Clinic ID (must match CLINIC_ID in backend/.env)
VITE_CLINIC_ID=clinic-001
